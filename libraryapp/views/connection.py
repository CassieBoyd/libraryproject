# This allows any view to import the path to the database so each can connect to it and query data.
class Connection:
    db_path = "/Users/clb/workspace/python/library-management/libraryproject/db.sqlite3"